namespace InviteTracker.Commands;

public enum ChannelType
{
    GuildText, Dm, GuildVoice, GroupDm, GuildCategory, GuildNews, GuildStore, GuildNewsThread, GuildPublicThread, GuildPrivateThread, GuildStageVoice
}